# پرامپت: ساخت ربات تلگرامی اخبار روزانه هوش مصنوعی («هوشکاو» / Hooshkav)

می‌خواهم یک پروژهٔ کامل و آماده‌اجرا بسازی که هر روز صبح ساعت ۸ (به‌وقت من)،
مهم‌ترین اخبار روز حوزهٔ هوش مصنوعی را از چند خبرگزاری معتبر جمع‌آوری کند،
با استفاده از یک API هوش مصنوعی خلاصه/فیلتر و به فارسی روان تبدیل کند، و
به‌صورت خودکار در کانال تلگرامم پست کند. لطفاً موارد زیر را دقیقاً رعایت کن:

## ۱. منابع خبری
- اخبار را از فید RSS چند خبرگزاری معتبر و شناخته‌شدهٔ فناوری/هوش مصنوعی جمع کن
  (مثل TechCrunch AI، VentureBeat AI، The Verge AI، Ars Technica، MIT Technology
  Review، Wired). لیست منابع باید در کد به‌شکل یک متغیر ساده و قابل‌ویرایش باشد.
- فقط اخبار ۲۴ تا ۳۰ ساعت اخیر را در نظر بگیر تا خبر تکراری یا قدیمی نفرستد.
- از هر منبع حداکثر چند خبر بگیر (برای کنترل حجم و هزینه)، نه کل فید.

## ۲. خلاصه‌سازی و انتخاب با هوش مصنوعی
- از API یک مدل زبانی (ترجیحاً Claude، مدل سبک و ارزان مثل Haiku چون فقط یک‌بار
  در روز و با حجم کم فراخوانی می‌شود) استفاده کن تا از بین همهٔ اخبار جمع‌آوری‌شده،
  حدود ۵ تا ۸ خبر مهم‌تر و غیرتکراری انتخاب و به فارسی خلاصه شود.
- هوش مصنوعی نباید خبر بسازد یا حدس بزند؛ فقط باید از بین اخبار واقعی که به آن
  داده شده انتخاب و خلاصه کند، و لینک هرکدام دقیقاً همان لینک اصلی خبر باشد.
- خروجی باید به‌شکل HTML سازگار با Telegram (`parse_mode=HTML`) باشد: تیتر با
  `<b>`، لینک هرکدام با `<a href="...">`، شماره‌گذاری‌شده، و کوتاه (یک جمله
  توضیح برای هر خبر).

## ۳. ارسال به تلگرام
- پیام نهایی باید با Telegram Bot API به یک کانال (پابلیک یا پرایوت) ارسال شود.
- اگر متن از محدودیت طول پیام تلگرام (۴۰۹۶ کاراکتر) بیشتر شد، به‌صورت خودکار به
  چند پیام تقسیم شود.
- اگر هیچ خبر جدیدی در بازهٔ زمانی موردنظر پیدا نشد، یک پیام ساده مبنی بر «خبر
  مهمی امروز نبود» ارسال شود، نه اینکه برنامه بدون خروجی متوقف شود.

## ۴. اجرای زمان‌بندی‌شده (بدون نیاز به سرور)
- اسکریپت باید طوری نوشته شود که با **GitHub Actions** به‌صورت رایگان و بدون
  نیاز به سرور شخصی، هر روز در ساعت مشخص (معادل ۸ صبح به‌وقت من) اجرا شود.
- تنظیمات حساس (کلید API، توکن ربات، Chat ID) باید از طریق GitHub Secrets /
  متغیرهای محیطی خوانده شوند، نه این‌که در کد هاردکد شوند.
- یک گزینهٔ اجرای دستی (workflow_dispatch) هم برای تست بدون نیاز به منتظرماندن
  تا فردا صبح اضافه کن.

## ۵. ساختار خروجی موردنیاز
- `main.py` (یا معادل آن): کد اصلی جمع‌آوری خبر → فراخوانی API هوش مصنوعی →
  ارسال به تلگرام، با مدیریت خطا (مثلاً اگر یک فید در دسترس نبود، بقیه ادامه پیدا کنند).
- `requirements.txt`: وابستگی‌های لازم.
- `.github/workflows/*.yml`: تعریف زمان‌بندی روزانه با cron (به‌وقت UTC، با توضیح
  دربارهٔ نحوهٔ تبدیل به منطقهٔ زمانی من).
- `README.md` به زبان فارسی با راهنمای کامل قدم‌به‌قدم:
  - چطور با BotFather ربات تلگرام بسازم و توکن بگیرم.
  - چطور ربات را ادمین کانال کنم و Chat ID کانال (پابلیک/پرایوت) را پیدا کنم.
  - چطور از Anthropic API Key بگیرم.
  - چطور Secrets را در گیت‌هاب تنظیم کنم.
  - چطور برای تست، workflow را دستی اجرا کنم.
  - چطور منابع خبری، تعداد خبرها، یا بازهٔ زمانی را شخصی‌سازی کنم.

## ۶. محدودیت‌ها و ترجیحات
- هزینهٔ استفاده از API هوش مصنوعی باید کم باشد (استفادهٔ روزانه با حجم کوچک
  ورودی/خروجی، نه فراخوانی سنگین یا مکرر).
- کد باید تمیز، قابل‌ویرایش، و بدون نیاز به دانش برنامه‌نویسی پیشرفته برای
  تنظیم اولیه باشد (فقط جایگذاری چند secret و آپلود در گیت‌هاب کافی باشد).
- توضیحات و README به فارسی باشد؛ کامنت‌های کد می‌تواند انگلیسی باشد.
